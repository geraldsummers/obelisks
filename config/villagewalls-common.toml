[automaticWalls]
	#Whether village walls can be generated automatically when village structures load.
	enabled = true
	#Chance from 0.0 to 1.0 that an automatically detected village gets walls. A skipped village is remembered so it is not rerolled on every chunk load.
	spawnChance = 0.5
